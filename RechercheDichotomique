def recherche(l, e):
    if l == []:
        return False
    if len(l)==1:
        return l[0]==e
    if l[len(l)//2]==e:
        return True
    if l[len(l)//2]>e:
        return recherche(l[:(len(l)//2)], e)
    return recherche(l[((len(l)+1)//2):], e)
